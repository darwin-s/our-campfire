logging:
  level:
    com.ourcampfire: trace
    root: info

spring:
  jpa:
    show-sql: on
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
